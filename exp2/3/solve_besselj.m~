J0 = @(x) besselj(0,x);
z=zeros
for n = 1:10
 z(n) = fzerotx(J0,[(n-1) n]*pi);
end
x = 0:pi/50:10*pi;
y = J0(x);
title("The first ten positive zero point of J");
plot(z,zeros(1,10),'o',x,y,'-')
line([0 10*pi],[0 0],'color','black')
axis([0 10*pi -0.5 1.0])